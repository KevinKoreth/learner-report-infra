apiVersion: v1
kind: Service
metadata:
  name: learner-report-cs-fe-service
spec:
  selector:
    app: learner-report-cs-fe
  ports:
    - protocol: TCP
      port: {{ .Values.frontend.servicePort }}
      targetPort: 3000
  type: ClusterIP
